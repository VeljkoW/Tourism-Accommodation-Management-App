<Window x:Class="BookingApp.View.Guest.Windows.GuestNotifications"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View.Guest.Windows"
        mc:Ignorable="d"
        Title="GuestNotifications" Height="400" Width="350" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Grid.Row="0" Content="New Notifications" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"></Label>
        <ListBox Grid.Column="0" Grid.Row="1" ItemsSource="{Binding ProcessedReschedulingRequests}" SelectedValuePath="PrintNotifications" DisplayMemberPath="PrintNotifications" FontSize="15"/>
    </Grid>
</Window>
